interface:
  display_name: "ISpy"
  short_description: "Repo-specific patterns and workflows for iSpy"
  default_prompt: "Use the iSpy repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true